[project]
name = "swing-scanner"
version = "1.0.0"
description = "Swing trading scanner with AI scoring"
dependencies = [
    "yfinance==0.2.54",
    "pandas==2.2.3",
    "numpy==1.26.4",
    "requests==2.32.3",
]

[tool.vercel]
entrypoint = "api.scan:handler"
